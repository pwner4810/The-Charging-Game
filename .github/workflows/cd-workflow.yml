name: CD Workflow

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Use reusable build action
        uses: ./.github/actions/build-action
        with:
          node-version: '18'

          #place to implement deployment
